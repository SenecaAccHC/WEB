<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Lego Collection</title>
  <link rel="stylesheet" href="/css/main.css" />
</head>

<body tabindex="-1">
  
  <%- include('partials/navbar', { page: '/lego/sets' }) %>

  <div class="container mx-auto">

    <div class="hero bg-base-200 rounded-lg p-6">
      <div class="hero-content text-center">
        <div class="max-w-md">
          <h1 class="text-5xl font-bold">Collection</h1>
          <p class="py-6">Browse the entire collection. Click the themes to filter by themes such as:</p>
          <div class="space-x-2">
            <a href="/lego/sets?theme=Technic" class="btn btn-sm btn-primary">Technic</a>
            <a href="/lego/sets?theme=Classic Town" class="btn btn-sm btn-primary">Classic Town</a>
            <a href="/lego/sets?theme=Supplemental" class="btn btn-sm btn-primary">Supplemental</a>
          </div>
        </div>
      </div>
    </div>

    <br />
    
    <div class="overflow-x-auto overflow-y-hidden">
      <table class="table table-zebra">
        <tbody>
          <% sets.forEach(set => { %>
            <tr>
              <td>
                <div class="avatar">
                  <div class="w-24 h-24 rounded-lg">
                    <img src="<%= set.img_url %>" alt="<%= set.name %>" />
                  </div>
                </div>
              </td>
              <td>
                <%= set.name %> |
                <a href="/lego/sets?theme=<%= set.Theme.name %>" class="btn mt-2 btn-xs"><%= set.Theme.name %></a>
              </td>
              <td>
                <span class="font-bold">Year:</span> <%= set.year %> -
                <span class="font-bold">Parts:</span> <%= set.num_parts %>
              </td>
              <th>
                <a href="/lego/sets/<%= set.set_num %>" class="btn btn-xs">Details</a>
              </th>
            </tr>
          <% }); %>
        </tbody>
      </table>
    </div>
  </div>
</body>

</html>
